import React from 'react';

const AddMedicineLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default AddMedicineLayout;